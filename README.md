# periodic-table-cli
Bash CLI tool that queries a PostgreSQL periodic table database and returns element information such as atomic number, symbol, type, atomic mass, melting point, and boiling point.
